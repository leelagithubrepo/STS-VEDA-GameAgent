[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "veda"
version = "0.1.0"
description = "Research-first autonomous game-playing agent foundation"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["veda*"]
